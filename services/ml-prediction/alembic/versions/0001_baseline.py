"""baseline schema (prediction + user domains)

Builds the full schema from the ORM metadata so the models stay the single source
of truth. Future schema changes get their own autogenerated revisions.

Revision ID: 0001_baseline
Revises:
Create Date: 2026-06-14
"""
from alembic import op

from app.store.models import Base

revision = "0001_baseline"
down_revision = None
branch_labels = None
depends_on = None


def upgrade() -> None:
    Base.metadata.create_all(bind=op.get_bind())


def downgrade() -> None:
    Base.metadata.drop_all(bind=op.get_bind())
